__pycache__/
*.pyc
.DS_Store
.venv/

# Tokens y webhooks: nunca al repo.
.env
*.token

# Caché de precios y estado del bot: regenerables, no van al repo.
# La barra inicial importa: sin ella el patrón matchea cualquier carpeta
# llamada 'data' a cualquier profundidad, incluida docs/data/ que SÍ se publica.
/data/

# Los resultados chicos SÍ se publican, para que cualquiera pueda verificar
# los números de la documentación. Solo se excluye el archivo pesado.
out/base_rate_trades.csv
